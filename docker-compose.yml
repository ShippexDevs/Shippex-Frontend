services:

  frontend:
    build:
      context: .

    container_name: shippex-frontend

    ports:
      - "5173:5173"

    volumes:
      - .:/app
      - /app/node_modules

    environment:
      CHOKIDAR_USEPOLLING: "true"

    restart: unless-stopped